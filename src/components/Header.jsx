import React from "react";
import {Container} from "react-bootstrap";

const Header = () => {
    return (
        <Container>
            <h1>todo-project</h1>
        </Container>
    );
};

export default Header;